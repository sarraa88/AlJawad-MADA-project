# Tables

This folder contains all tables generated during the analysis.

Tables are created programmatically using scripts in the `code/` directory and include summary statistics, regression results, and model comparison outputs.

Tables are stored in formats such as `.csv` and `.rds`.

Some tables are included in the manuscript, while others are generated as intermediate or supporting results.

All tables can be reproduced by running the analysis scripts.