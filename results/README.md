# Results

This folder and its subfolders contain all outputs generated during the analysis.

These include figures, tables, and intermediate results created programmatically by scripts in the `code/` directory.

Subfolders organize results by type, such as:
- `figures/` for plots and visualizations
- `tables/` for summary and model output tables

All results can be reproduced by running the analysis scripts.