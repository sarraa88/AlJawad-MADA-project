# large-files

This folder is for output files that are so large that they cannot be tracked with GitHub. That's generally anything above around 20MB. 

Currently, no large files are used in this project.